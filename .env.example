# Local Supabase stack (`supabase start` prints these).
# The anon key is designed to be public — RLS is what protects the data.
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=
